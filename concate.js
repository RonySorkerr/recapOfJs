const firstName = 'rony';
const lastName = 'sarakr';
const age = 22;
const gender = 'male';

const fullInfo = `Mr . ${firstName} ${lastName} your age is ${age} and your gender is ${gender}`;
console.log(fullInfo);